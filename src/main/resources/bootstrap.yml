spring:
  application:
    name: order-service

  cloud:
    config:
      uri: http://localhost:9000

  profiles:
    active: dev

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

#  management:
#    endpoints:
#      web:
#        exposure:
#          include: '*'