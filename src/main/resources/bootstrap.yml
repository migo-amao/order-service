spring:
  application:
    name: account-service

  cloud:
    config:
      uri: ${CONFIG_SERVER_URI:http://localhost:9000}

server:
  port: 0

---

spring:
  profiles: container
server:
  port: 8080